info:
  name: Iniciar sesión
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "username": "admineliza@turnofacil.com",
        "password": "eliza123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
